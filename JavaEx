public static int sumFirstAndLastDigit(int number){
        int last=number%10,first;
        if(number<0)
            return -1;
        else
        {
            while(number>=10)
            {  number=number/10;
            }
            first=number;
            return (first+last);
        }
    }
